{
  "name": "part4",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "NODE_ENV=test jest --verbose --runInBand",
    "watch": "nodemon index.js",
    "build:ui": "rm -rf build && cd /Users/joelhassan/tkt/fullstackcourse/part5/bloglist-frontend/ && npm run build --prod && cp -r build /Users/joelhassan/tkt/fullstackcourse/part4/",
    "deploy": "git push heroku master",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push heroku master && npm run deploy",
    "logs:prod": "heroku logs --tail"
  },
  "author": "Joel Hassan",
  "license": "MIT",
  "dependencies": {
    "bcrypt": "^3.0.6",
    "cors": "^2.8.5",
    "dotenv": "^8.0.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.6.7",
    "mongoose-unique-validator": "^2.0.3"
  },
  "devDependencies": {
    "jest": "^24.8.0",
    "lodash": "^4.17.15",
    "nodemon": "^1.19.1",
    "supertest": "^4.0.2"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
